import React from 'react';
import ReactDOM from 'react-dom';
import style from '../css-modules/add.module.css'

const Add = ()=>{

    return(
        <div className={style.container} >
            <button className={style.button}>Add</button>
        </div>
    )

}

export default Add;